# Git finalization exceptions

Audit date: 2026-08-28.

## Large historical object

The staged tree contains one blob above 100 MiB:

- `archive/miscellaneous/.jz_transfer_20260817/subsonic_article_results_20260817.tar.gz`
- size: 302,188,869 bytes
- Git OID: `3af2723f098c272948d28eed415fc1c466905667`

This is an existing local-history blob, not a newly created staged blob. It is
not reachable from the currently fetched `origin` refs. Per the migration
policy, it is not converted to Git LFS and is not deleted locally. Publication
must therefore be confirmed by the actual non-force push.

`NEW STAGED BLOBS >=100 MiB = 0`.

## Whitespace check

`git diff --cached --check` reports pre-existing CRLF/trailing whitespace in
33 migrated data, manifest, and legacy report files. These files are preserved
byte-for-byte or generated by the migration inventory; they are not normalized
because that would alter archived/scientific data solely to silence Git's
whitespace heuristic.

This exception does not concern executable Python or active launcher syntax,
which passed their dedicated checks.
